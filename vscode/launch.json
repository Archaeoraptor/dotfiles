{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/build/${fileBasenameNoExtension}.out",
            "cwd": "${fileDirname}",
            "args": [
                "&&",
                "exit"
            ],
            "preLaunchTask": "Compile"
        }
    ]
}